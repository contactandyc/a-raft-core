project_title: a-raft-library
version: "0.1.1"
description: "A dynamically scaffolded c/cmake project"
stack: c/cmake
date_created: 2026-04-26

profile: default

# ── 1. Scaffolding Engine Source ──
base_templates:
  repo: https://github.com/contactandyc/scaffold-templates.git
  ref: main

# ── Dynamic Setup Variables ──
c_standard: "23"
cxx_standard: "17"

# ── 3. Licenses & Legal ──
# license_profile: apache-2.0
# license_overrides:
#   "src/vendor/**": mit

# ── 4. Dependencies ──
depends_on: []

examples:
  multi-raft:
    targets:
      multi-raft:
        - src/main.c
